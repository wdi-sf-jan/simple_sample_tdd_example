var SimpleSample = {
  addTwoNumbers: function(x, y) {
    // get the test to pass
  },

  subtractTwoNumbers: function(x, y) {
    // remember, write the test first
    // then write the code to get the test to pass
  }
};

module.exports = SimpleSample;
